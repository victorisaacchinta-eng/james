"""JAMES Workflow Lab: a separate, locally runnable app for the evidence, guidance and
memory workflows. Reuses james_core (WARDEN and the pack loader) read-only.
All data is synthetic."""
__version__ = "0.1.0"
